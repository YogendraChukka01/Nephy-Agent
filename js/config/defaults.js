/* =====================================================
   CONFIG — defaults
===================================================== */

export const DEFAULT_SETTINGS = {
  baseUrl: "https://api.openai.com/v1",
  apiKey: "",
  model: "gpt-4o",
  customModels: "",
  systemPrompt: "You are Leazed AI, a helpful, accurate and concise AI assistant.",
  temperature: 0.7,
  stream: true,
  theme: "system",
  enterToSend: true,
  density: "comfortable"
};

export const DEFAULT_UI = {
  sidebarCollapsed: false
};